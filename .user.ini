; PHP configuration for large file uploads
; This file works with PHP-FPM (most modern hosting)
; If your server uses Apache with mod_php, the .htaccess settings will be used instead

upload_max_filesize = 100M
post_max_size = 100M
memory_limit = 256M
max_execution_time = 300
max_input_time = 300

